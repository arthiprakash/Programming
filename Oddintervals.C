#include<stdio.h>
#include<conio.h>
main()
{
int a,b,i;
printf("Enter the initial range:");
scanf("%d",&a);
printf("Enter the final range:");
scanf("%d",&b);
for(i=a;i<=b;i++)
{
if(i%2!=0)
printf("%d",i)}
}
